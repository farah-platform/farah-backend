<div align="right">

# قواعد العمل على الريبو — منصّة فرح

هذا الملف يحدّد كيف يشتغل الفريق (8 أشخاص) على نفس الكود بدون تعارضات ولا شغل ضائع. **اقرأه قبل أول كوميت.**

---

## 🔗 القاعدة الذهبية: كل شغل مربوط بالـ SRS

وثيقة الـ SRS فيها 45 متطلباً وظيفياً (`FR-01` … `FR-45`) و33 قصة مستخدم (`US-01` … `US-33`) و9 مجموعات عمل (`EP-1` … `EP-9`).

**كل فرع، وكل كوميت، وكل Issue، وكل Pull Request لازم يحمل معرّف القصة.** السبب: لما يسأل الدكتور أو أحد الزملاء "وين تنفيذ FR-30؟" يكون الجواب موجوداً في تاريخ الريبو نفسه، لا في ذاكرة أحد.

---

## 🌿 الفروع (Branches)

| الفرع | الغرض | من يدفع عليه |
|---|---|---|
| `main` | الكود المستقر القابل للعرض | **لا أحد مباشرة** — عبر PR فقط |
| `develop` | التكامل اليومي بين الأعضاء | عبر PR فقط |
| `feature/*` | تطوير قصة واحدة | صاحب القصة |
| `fix/*` | إصلاح خطأ | من يصلحه |

### تسمية الفرع

```
feature/US-08-send-booking-request
feature/US-26-package-items
fix/US-14-proof-review-timeout
```

الصيغة: `النوع/معرّف-القصة-وصف-قصير-بالإنجليزي`

> **فرع واحد لكل قصة.** لا تجمع قصتين في فرع واحد حتى لو كانتا متقاربتين — المراجعة تصير مستحيلة والرجوع عن خطأ يجرّ معه شغلاً سليماً.

---

## ✍️ الكوميتات (Commits)

الصيغة المعتمدة:

```
<type>(<scope>): <US-xx> وصف مختصر
```

**أمثلة:**

```bash
feat(booking): US-08 إرسال طلب الحجز وتقسيمه لطلبات فرعية
feat(payment): US-13 رفع إثبات التحويل مع التحقق من الصيغة والحجم
fix(auth): US-22 تصحيح صلاحية رمز التحقق لـ 10 دقائق
docs(srs): تحديث مصفوفة التتبع
style(ui): US-04 ضبط عرض التصنيفات على RTL
refactor(api): US-30 فصل منطق توريد المستحقات
test(booking): US-27 اختبارات مسار رفض المزوّد
```

**الأنواع:** `feat` ميزة · `fix` إصلاح · `docs` توثيق · `style` تنسيق · `refactor` إعادة هيكلة · `test` اختبارات · `chore` مهام عامة

### قواعد
- كوميت واحد = تغيير منطقي واحد. لا `تعديلات` ولا `update` ولا `اخر تعديل`.
- اكتب الوصف بصيغة **ما الذي يفعله الكوميت**، لا ما الذي كنت تحاول فعله.
- لا ترفع ملفات `.env` ولا مفاتيح ولا بيانات بنكية حقيقية — أبداً.

---

## 🔄 دورة العمل اليومية

```bash
# 1. حدّث develop عندك قبل ما تبدأ
git checkout develop
git pull origin develop

# 2. افتح فرعك من develop
git checkout -b feature/US-08-send-booking-request

# 3. اشتغل واعمل كوميتات صغيرة
git add .
git commit -m "feat(booking): US-08 إرسال طلب الحجز"

# 4. قبل ما ترفع، اسحب آخر develop وادمجه عندك
git pull origin develop

# 5. ارفع فرعك
git push -u origin feature/US-08-send-booking-request

# 6. افتح Pull Request من GitHub باتجاه develop
```

> **اسحب `develop` كل يوم صباحاً.** الفرع اللي يقعد أسبوعاً بدون تحديث يتحول إلى كابوس تعارضات وقت الدمج.

---

## 🔍 الـ Pull Request

**لا يُدمج أي PR إلا بموافقة شخص واحد على الأقل غير كاتبه.**

عنوان الـ PR:
```
US-08: إرسال طلب الحجز
```

وصف الـ PR لازم يحتوي:

```markdown
## القصة
US-08 — إضافة الخدمات وإرسال طلب الحجز

## المتطلبات المنفَّذة
FR-08

## ماذا تم
- تقسيم الطلب متعدد المزوّدين إلى طلبات فرعية
- التحقق من اختيار خدمة واحدة على الأقل

## معايير القبول المتحقَّقة
- [x] لا يمكن الإرسال دون اختيار خدمة وتحديد تاريخ الفرح
- [x] يصل الطلب لكل مزوّد معنيّ

## كيف يُختبر
1. ...
2. ...
```

### للمُراجِع
افتح الـ SRS على القصة المذكورة وتأكد أن **معايير القبول** متحققة فعلاً. لا تكتفِ بـ "الكود يبدو تمام" — المعيار المكتوب هو المرجع.

---

## 🏷️ الـ Issues والتنظيم

افتح Issue لكل قصة، وسمِّها بمعرّفها، واستخدم الـ Labels:

| Label | المعنى |
|---|---|
| `EP-1` … `EP-9` | المجموعة (Epic) التي تنتمي لها |
| `backend` `frontend` `design` | التخصص |
| `critical` `high` `medium` | الأولوية كما في الـ SRS |
| `blocked` | متوقف على قصة أخرى أو قرار من العميل |

استخدموا **GitHub Projects** كلوحة: `للتنفيذ ← قيد العمل ← قيد المراجعة ← منجز`.

---

## ⚠️ ممنوعات

| ❌ | لماذا |
|---|---|
| الدفع مباشرة على `main` أو `develop` | يكسر الكود على الجميع بلا مراجعة |
| `git push --force` على فرع مشترك | يمسح شغل زملائك نهائياً |
| رفع `.env` أو مفاتيح أو كلمات مرور | تسريب أمني حقيقي حتى لو حذفته لاحقاً |
| رفع `node_modules` أو `vendor` | يضخّم الريبو بلا فائدة |
| فرع بدون معرّف قصة | يضيّع التتبع الذي بُنيت عليه الوثيقة |
| كوميت بحجم يوم كامل من الشغل | لا أحد يستطيع مراجعته فعلياً |

---

## 🆘 عند التعارض (Conflict)

لا تحذف شغل حدا ولا تقبل "خُذ نسختي" بشكل أعمى. افتح الملف، اقرأ الجزأين، وإذا ما كان واضحاً أيّهما الصحيح **اسأل صاحب الكود الثاني قبل ما تحلّ التعارض**. دقيقة سؤال أرخص من يوم إصلاح.

</div>
